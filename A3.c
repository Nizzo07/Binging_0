#include <stdio.h>
#include <stdlib.h>

int main () 
{    
    int n;    
    char chave[8];
    char mensage[n];

    if(scanf("%d", &n) != 1){
        return 1;
    }

    for(int i = 0; i < n; i++)
    {
        scanf("%s", chave);
    }

    

    return 0;
}